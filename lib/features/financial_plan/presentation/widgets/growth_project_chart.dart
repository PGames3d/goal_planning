import 'package:flutter/material.dart';

class GrowthProjectChart extends StatelessWidget {
  const GrowthProjectChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
